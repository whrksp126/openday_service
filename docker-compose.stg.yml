services:
  nextjs:
    container_name: openday_nextjs_stg
    image: whrksp126/openday_service:stg
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_KAKAO_JS_KEY: ${NEXT_PUBLIC_KAKAO_JS_KEY:-}
        NEXT_PUBLIC_KAKAO_TEMPLATE_ID: ${NEXT_PUBLIC_KAKAO_TEMPLATE_ID:-}
    env_file: .env.stg
    environment:
      NODE_ENV: production
    volumes:
      - uploads_stg_data:/app/public/uploads
    networks:
      - openday_stg
      - nginx_proxy
    restart: always
    depends_on:
      mysql:
        condition: service_healthy

  mysql:
    container_name: openday_mysql_stg
    image: mysql:8.0
    env_file: .env.stg
    volumes:
      - mysql_stg_data:/var/lib/mysql
    networks:
      - openday_stg
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  openday_stg:
    driver: bridge
  nginx_proxy:
    external: true

volumes:
  mysql_stg_data:
  uploads_stg_data:
